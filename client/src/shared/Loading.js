import React from "react";

import Overlay from "./Overlay";

const Loading = () => {
    return (
        <Overlay className="overlay">
            <h1>Loading...</h1>
        </Overlay>
    );
};

export default Loading;
